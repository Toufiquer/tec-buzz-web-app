Look at the page '/dashboard/admin/menu/edit?menuItem=menu-1' and update the page as the following instructions. (Only Edit Menu 1)

1. Logo Tab:
    - Dispaly the Logo.
    - Remove/Uplod Logo. (Choose from Media if not found then uplod, after upload add corp, zoom, and three ration 1:1, 16:9, full)
    - Change Position Mobile: 
        -- Padding-X
        -- Padding-Y
        -- Margin-X
        -- Margin-Y
    - Add a  Visual Option so I can see Visual Edits.

2. Brand Name Tab:
    - Display Brand Name
    - Change Color, Font-size(Choose from xs,sm,md,xl,2xl,3xl,4xl), Font-family (Choose from 10 font-family), 
    - Add a  Visual Option so I can see Visual Edits.
    
3. Theme Tab:
    - Menu Position (Choose from Fixed, Sticky, Scroll)
    - Change Background Color, Font-size(Choose from xs,sm,md,xl,2xl,3xl,4xl), Font-family (Choose from 10 font-family), 
    - Add Background Transparency [0-100]
    - Add a  Visual Option so I can see Visual Edits.

4. Button Tab:
    - Choose Button option (Login/dashboard[sync with session], Continue[Name, path] )
    - if choose Name, path then add two input field Name, and path
    - add icon option. (choose from all-icons)
    - add switch for display icon
    - add switch for Transparency Background. if solid then add option for  choose color, if  Transparency then add option [0-100]
    - Padding-
    - Padding-
    - Margin-
    - Margin-
    - Border (Choose from None, xs, sm, md, xl)
    - Border Radious (Choose from None, xs, sm, md, xl, 2xl, Full)
    - Add a  Visual Option so I can see Visual Edits.

5. Mobile Tab:
    - at the top add a switch for enable custom Mobile Menu. if on then render it only in Mobile view.
    - at the left  select two option Grid View, Flex View.
    - if choose Grid View then display Grid Layout [2*2,2*3,3*2,3*3] items
    - if choose Flex View.  then display [4,6,9] items
    - add and option for Add new  item and Edit or Delete item.
    - Add a  Visual Option so I can see Visual Edits.

6. Main Menu Tab:
    - remove this tab and  move it at the bottom and  it will always visible.
    - add new item option,
    - I can move each item  up, down. and can edit, delete or view each item. and add sub-item, also add the same features in the sub-sub-item.

---------------------------------------------
Update 'Mobile' tab inside '/dashboard/admin/menu/edit?menuItem=menu-1', '/dashboard/admin/menu/edit?menuItem=menu-2', and '/dashboard/admin/menu/edit?menuItem=menu-3'  as the following instructions.

1. at the top there is a switch for Enable custom Mobile Menu. and if it on then this menu is render only in Mobile device. 
2. Total 4 Main Section 
    A. View Style
        - Grid View
        - Flex View 
    B. Grid Layout
        - 2*2
        - 2*3
        - 3*2 
        - 3*3
    C. Mobile Menu  
        - "Add" button it will open a pop-up, and I can add New Item from here. inside the modal please add those field [Name, path, icon, Hideicon] and make sure I can choose icon from modal. and Hideicon throw switch.

    D. Preview.
        - make a preview of the Mobile Layout.
    
    * and based on Grid Layout select pelase update default item opitons. 