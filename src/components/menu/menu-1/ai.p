Look at the file 'src/components/menu/menu-1/...' and update it  with the following instructions.
- In the Mobile View:
    -- remove the border.
    -- If there is icon and image then display as flex. so icon and text will be at the same row.
    -- Make default White Background color.
    -- if I hover an menu then it will display grid same as the picture.

